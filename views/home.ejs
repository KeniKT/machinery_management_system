<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Home | Machinery Management</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <style>
      :root {
        --primary: #1b3c6b;
        --accent: #5c7cfa;
        --light: #f8f9fa;
        --dark: #2d3e50;
        --gray: #cfd8e3;
        --text-dark: #1e2a38;
        --text-muted: #555;
      }

      html,
      body {
        height: 100%;
      }

      body {
        margin: 0;
        display: flex;
        flex-direction: column;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: var(--primary);
        color: white;
      }

      main {
        flex: 1;
      }

      .menu-btn {
        position: fixed;
        top: 15px;
        left: 15px;
        z-index: 1050;
        border-radius: 50%;
        width: 50px;
        height: 50px;
        background-color: var(--accent);
        color: white;
        border: none;
        display: flex;
        justify-content: center;
        align-items: center;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        transition: background-color 0.3s ease;
      }

      .menu-btn:hover {
        background-color: #445ee2;
      }

      .offcanvas-start {
        width: 260px !important;
        background-color: var(--primary);
        color: white;
        box-shadow: 4px 0 8px rgba(0, 0, 0, 0.3);
      }

      .offcanvas-header {
        background-color: var(--dark);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      }

      .nav-link {
        color: #f1f1f1;
        font-size: 1rem;
        padding: 12px 18px;
        border-radius: 6px;
        transition: background 0.3s ease, color 0.3s ease;
        font-weight: 500;
      }

      .nav-link:hover,
      .nav-link.active {
        background-color: var(--accent);
        font-weight: 600;
        color: white;
      }

      .container.text-center.my-5 h1 {
        font-weight: 700;
        font-size: 2.8rem;
        color: var(--gray);
        text-shadow: 0 2px 6px rgba(0, 0, 0, 0.25);
      }

      .container.text-center.my-5 p {
        font-size: 1.25rem;
        color: var(--gray);
        max-width: 650px;
        margin-left: auto;
        margin-right: auto;
        line-height: 1.5;
      }

      .feature-card {
        background: #fff;
        color: var(--text-dark);
        padding: 40px 30px;
        border-radius: 16px;
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.12);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        height: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        cursor: default;
      }

      .feature-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.18);
      }

      .feature-card h5 {
        font-size: 1.5rem;
        margin-top: 20px;
        margin-bottom: 12px;
        color: var(--primary);
        font-weight: 700;
      }

      .feature-card p {
        font-size: 1.05rem;
        color: var(--text-muted);
        line-height: 1.5;
        font-weight: 500;
        max-width: 280px;
      }

      .card-icon {
        font-size: 2.8rem;
        color: var(--accent);
        filter: drop-shadow(0 2px 3px rgba(0, 0, 0, 0.1));
        user-select: none;
      }

      footer.footer {
        background-color: #0f2847;
        color: #ddd;
        padding: 20px 0;
        text-align: center;
        font-size: 0.95rem;
        user-select: none;
      }

      @media (max-width: 768px) {
        .container.text-center.my-5 h1 {
          font-size: 2rem;
        }

        .container.text-center.my-5 p {
          font-size: 1rem;
        }

        .feature-card p {
          max-width: 100%;
        }
      }
    </style>
  </head>

  <body>
    <%- include('partials/drawer') %>

    <main>
      <div class="container text-center my-5">
        <h1 class="mb-3">Welcome to the Machinery Management System</h1>
        <p class="lead mb-3">
          Streamline your equipment tracking, maintenance, and monitoring — all
          from one place.
        </p>
      </div>

      <div class="container mb-5">
        <div class="row g-4">
          <div class="col-md-3 col-sm-6">
            <div class="feature-card">
              <i class="fas fa-cogs card-icon"></i>
              <h5>Machinery Inventory</h5>
              <p>
                <strong><%= stats.total_machines %></strong> total machines
                registered.
              </p>
            </div>
          </div>

          <div class="col-md-3 col-sm-6">
            <div class="feature-card">
              <i class="fas fa-tools card-icon"></i>
              <h5>Maintenance Tasks</h5>
              <p>
                <strong><%= stats.total_tasks %></strong> total tasks<br />
                <strong><%= stats.pending %></strong> pending,<br />
                <strong><%= stats.in_progress %></strong> in progress,<br />
                <strong><%= stats.done %></strong> done
              </p>
            </div>
          </div>

          <div class="col-md-3 col-sm-6">
            <div class="feature-card">
              <i class="fas fa-users-cog card-icon"></i>
              <h5>Technician Task Panel</h5>
              <p>
                <strong><%= stats.total_technicians %></strong> technicians
                registered
              </p>
            </div>
          </div>

          <div class="col-md-3 col-sm-6">
            <div class="feature-card">
              <i class="fas fa-chart-line card-icon"></i>
              <h5>Performance Monitoring</h5>
              <p>
                <strong><%= stats.completed_repairs %></strong> repairs
                completed<br />
                <strong><%= stats.active_repairs %></strong> currently active
              </p>
            </div>
          </div>
        </div>
      </div>
    </main>

    <footer class="footer">
      <p>&copy; 2025 Machinery Dashboard. All rights reserved.</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
