<!DOCTYPE html>
<html>
<head>
  <title>Edit Machine</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">Edit Machine</h1>
    <form action="/machines/<%= machine.id %>?_method=PUT" method="POST">
      <div class="mb-3">
        <label class="form-label">Name:</label>
        <input class="form-control" type="text" name="name" value="<%= machine.name %>" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Type:</label>
        <input class="form-control" type="text" name="type" value="<%= machine.type %>" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Status:</label>
        <input class="form-control" type="text" name="status" value="<%= machine.status %>" required>
      </div>
      <button class="btn btn-primary" type="submit">Update</button>
      <a href="/machines" class="btn btn-secondary ms-2">Cancel</a>
    </form>
  </div>
</body>
</html>
