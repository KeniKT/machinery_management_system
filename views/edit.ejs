<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Machine</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />

  <style>
  :root {
    --primary: #1b3c6b;
    --primary-dark: #0f2847;
    --primary-light: #2d4d84;
    --accent: #5c7cfa;
    --accent-dark: #445ee2;
    --accent-light: #7d95ff;
    --light: #f8f9fa;
    --dark: #2d3e50;
    --text-dark: #1e2a38;
    --white: #ffffff;
    --gray: #cfd8e3;
  }

  body {
    background: linear-gradient(135deg, var(--primary), var(--primary-dark));
    color: var(--white);
    min-height: 100vh;
    margin: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    font-family: 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    position: relative;
  }

  body::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
      radial-gradient(circle at 20% 30%, rgba(92, 124, 250, 0.15) 0%, transparent 40%),
      radial-gradient(circle at 80% 70%, rgba(92, 124, 250, 0.1) 0%, transparent 40%);
    z-index: -1;
    pointer-events: none;
  }

  .container {
    background-color: rgba(255, 255, 255, 0.95);
    color: var(--text-dark);
    padding: 2.5rem;
    border-radius: 16px;
    max-width: 600px;
    width: 100%;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    backdrop-filter: blur(8px);
    border: 1px solid rgba(255, 255, 255, 0.3);
  }

  h1 {
    color: var(--primary);
    font-weight: 700;
    font-size: 1.8rem;
    text-align: center;
    margin-bottom: 2rem;
    position: relative;
    padding-bottom: 0.5rem;
  }

  h1::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 4px;
    background: linear-gradient(90deg, var(--accent), var(--accent-light));
    border-radius: 2px;
  }

  .form-label {
    font-weight: 600;
    color: var(--primary-dark);
  }

  .form-control, .form-select {
    border-radius: 8px;
    padding: 0.75rem;
    font-size: 1rem;
    border: 1px solid var(--gray);
    transition: border-color 0.3s;
  }

  .form-control:focus, .form-select:focus {
    border-color: var(--accent);
    box-shadow: 0 0 0 0.2rem rgba(92, 124, 250, 0.25);
  }

  .btn-primary {
    background: linear-gradient(to right, var(--accent), var(--accent-dark));
    border: none;
    color: var(--white);
    font-weight: 500;
    padding: 0.6rem 1.4rem;
    border-radius: 8px;
    transition: all 0.3s ease;
    width: 100%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .btn-primary:hover {
    background: linear-gradient(to right, var(--accent-light), var(--accent));
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(92, 124, 250, 0.4);
  }

  .btn-secondary {
    background: linear-gradient(to right, var(--primary), var(--primary-dark));
    color: var(--white);
    font-weight: 500;
    padding: 0.6rem 1.4rem;
    border-radius: 8px;
    transition: all 0.3s ease;
    width: 100%;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .btn-secondary:hover {
    background: linear-gradient(to right, var(--primary-light), var(--primary));
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(27, 60, 107, 0.4);
    color: var(--white);
  }

  button i, a i {
    margin-right: 0.4rem;
  }

  @media (max-width: 576px) {
    .container {
      padding: 1.5rem;
    }

    h1 {
      font-size: 1.5rem;
    }
  }
</style>

</head>
<body>

  <div class="container">
    <h1>✏️ Edit Machine</h1>
    <form action="/machines/<%= machine.id %>" method="POST">
  <input type="hidden" name="_method" value="PUT">

  <div class="mb-3">
    <label class="form-label">Machine Name</label>
    <input class="form-control" 
           type="text" 
           name="name" 
           value="<%= machine.name %>" 
           required>
  </div>

  <select class="form-select" name="type" required>
  <option value="" disabled>Select machine type</option>
  <option value="Excavator" <%= machine.type === 'Excavator' ? 'selected' : '' %>>Excavator</option>
  <option value="Bulldozer" <%= machine.type === 'Bulldozer' ? 'selected' : '' %>>Bulldozer</option>
  <option value="Crane" <%= machine.type === 'Crane' ? 'selected' : '' %>>Crane</option>
  <option value="Loader" <%= machine.type === 'Loader' ? 'selected' : '' %>>Loader</option>
  <option value="Grader" <%= machine.type === 'Grader' ? 'selected' : '' %>>Grader</option>
  <option value="Backhoe" <%= machine.type === 'Backhoe' ? 'selected' : '' %>>Backhoe</option>
  <option value="Compactor" <%= machine.type === 'Compactor' ? 'selected' : '' %>>Compactor</option>
v  <option value="Trencher" <%= machine.type === 'Trencher' ? 'selected' : '' %>>Trencher</option>
  <option value="Drilling Rig" <%= machine.type === 'Drilling Rig' ? 'selected' : '' %>>Drilling Rig</option>

  <!-- Add other options similarly -->
</select>

  <div class="mb-3">
    <label class="form-label">	Model</label>
    <input class="form-control" 
           type="text" 
           name="model" 
           value="<%= machine.model %>" 
           required>
  </div>

  <button class="btn btn-primary mb-2" type="submit">
    <i class="fas fa-save"></i> Update Machine
  </button>
  <a href="/machines" class="btn btn-secondary">
    <i class="fas fa-arrow-left"></i> Cancel
  </a>
</form>
  </div>

</body>
</html>
