<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Welcome | Machinery Management</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
 :root {
  --primary-color: #6366f1;
  --secondary-color: #8b5cf6;
  --accent-color: #06b6d4;
  --dark-color: #0f172a;
  --darker-color: #020617;
  --light-color: #f8fafc;
  --glass-bg: rgba(255, 255, 255, 0.1);
  --glass-border: rgba(255, 255, 255, 0.2);
  --shadow-light: rgba(99, 102, 241, 0.3);
  --shadow-dark: rgba(0, 0, 0, 0.4);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body {
  height: 100%;
  font-family: 'Poppins', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  background: radial-gradient(ellipse at top, #1e293b 0%, var(--darker-color) 100%);
  color: var(--light-color);
  overflow-x: hidden;
  scroll-behavior: smooth;
}

.container-wrapper {
  min-height: 100vh;
  position: relative;
  overflow: hidden;
}

/* Enhanced animated background */
.bg-circle {
  position: absolute;
  border-radius: 50%;
  background: linear-gradient(135deg, 
    rgba(99, 102, 241, 0.15) 0%, 
    rgba(139, 92, 246, 0.1) 50%, 
    rgba(6, 182, 212, 0.05) 100%);
  z-index: 0;
  filter: blur(1px);
}

.circle-1 {
  width: 800px;
  height: 800px;
  top: -400px;
  right: -400px;
  animation: floatRotate 20s linear infinite;
}

.circle-2 {
  width: 600px;
  height: 600px;
  bottom: -300px;
  left: -300px;
  animation: floatRotate 25s linear infinite reverse;
}

.bg-circle::before {
  content: '';
  position: absolute;
  inset: 20px;
  border-radius: 50%;
  background: linear-gradient(45deg, 
    rgba(6, 182, 212, 0.1), 
    rgba(139, 92, 246, 0.05));
  animation: pulse 8s ease-in-out infinite;
}

@keyframes floatRotate {
  0% { transform: translateY(0) rotate(0deg) scale(1); }
  33% { transform: translateY(-30px) rotate(120deg) scale(1.05); }
  66% { transform: translateY(15px) rotate(240deg) scale(0.95); }
  100% { transform: translateY(0) rotate(360deg) scale(1); }
}

@keyframes pulse {
  0%, 100% { opacity: 0.3; transform: scale(1); }
  50% { opacity: 0.6; transform: scale(1.1); }
}

/* Enhanced header */
.login-btns {
  position: fixed;
  top: 25px;
  right: 25px;
  display: flex;
  gap: 12px;
  z-index: 1000;
}

.login-btn {
  background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
  color: white;
  font-weight: 600;
  font-size: 0.95rem;
  padding: 12px 24px;
  border: none;
  border-radius: 25px;
  text-decoration: none;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  box-shadow: 0 4px 20px var(--shadow-light);
  position: relative;
  overflow: hidden;
  backdrop-filter: blur(10px);
  border: 1px solid var(--glass-border);
}

.login-btn::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
  transition: left 0.6s ease;
}

.login-btn:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 8px 30px var(--shadow-light);
}

.login-btn:hover::before {
  left: 100%;
}

/* Enhanced main content */
.main-content {
  position: relative;
  z-index: 10;
  padding: 80px 20px 40px;
  max-width: 1400px;
  margin: 0 auto;
}

.hero-section {
  text-align: center;
  margin-bottom: 80px;
  animation: fadeInUp 1.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
}

.hero-section h1 {
  font-size: clamp(2.5rem, 5vw, 4.5rem);
  font-weight: 800;
  margin-bottom: 30px;
  background: linear-gradient(135deg, 
    var(--accent-color) 0%, 
    #ffffff 30%, 
    var(--primary-color) 70%, 
    var(--secondary-color) 100%);
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
  line-height: 1.1;
  text-shadow: 0 0 50px rgba(99, 102, 241, 0.3);
  animation: textGlow 3s ease-in-out infinite alternate;
}

@keyframes textGlow {
  0% { filter: brightness(1); }
  100% { filter: brightness(1.2); }
}

.hero-section p {
  font-size: clamp(1.1rem, 2vw, 1.4rem);
  line-height: 1.8;
  margin-bottom: 40px;
  max-width: 800px;
  margin-left: auto;
  margin-right: auto;
  opacity: 0.9;
  font-weight: 300;
}

/* Enhanced feature grid */
.feature-section {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
  gap: 25px;
  margin-bottom: 80px;
  padding: 0 20px;
}

.feature-card {
  background: var(--glass-bg);
  backdrop-filter: blur(20px);
  border-radius: 20px;
  padding: 35px;
  transition: all 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  border: 1px solid var(--glass-border);
  box-shadow: 0 8px 40px rgba(0, 0, 0, 0.2);
  position: relative;
  overflow: hidden;
  animation: slideInUp 0.8s ease-out;
  animation-fill-mode: both;
}

.feature-card:nth-child(1) { animation-delay: 0.1s; }
.feature-card:nth-child(2) { animation-delay: 0.2s; }
.feature-card:nth-child(3) { animation-delay: 0.3s; }
.feature-card:nth-child(4) { animation-delay: 0.4s; }
.feature-card:nth-child(5) { animation-delay: 0.5s; }
.feature-card:nth-child(6) { animation-delay: 0.6s; }

.feature-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: linear-gradient(90deg, var(--primary-color), var(--accent-color));
  transform: scaleX(0);
  transform-origin: left;
  transition: transform 0.4s ease;
}

.feature-card:hover {
  transform: translateY(-15px) scale(1.02);
  background: rgba(255, 255, 255, 0.15);
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
}

.feature-card:hover::before {
  transform: scaleX(1);
}

.feature-card h3 {
  font-size: 1.4rem;
  font-weight: 600;
  margin-bottom: 18px;
  color: var(--accent-color);
  display: flex;
  align-items: center;
  gap: 12px;
}

.feature-card h3 i {
  font-size: 1.6rem;
  background: linear-gradient(135deg, var(--primary-color), var(--accent-color));
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
  animation: iconPulse 2s ease-in-out infinite;
}

@keyframes iconPulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.1); }
}

.feature-card p {
  opacity: 0.85;
  line-height: 1.7;
  font-weight: 300;
  font-size: 1rem;
}

/* Enhanced CTA section */
.cta-section {
  text-align: center;
  animation: fadeIn 1.5s ease-out 0.8s both;
  padding: 0 20px;
}

.cta-box {
  background: linear-gradient(135deg, 
    rgba(99, 102, 241, 0.2) 0%, 
    rgba(139, 92, 246, 0.15) 50%, 
    rgba(6, 182, 212, 0.1) 100%);
  border-radius: 25px;
  padding: 50px 40px;
  border: 2px solid var(--glass-border);
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  max-width: 900px;
  margin: 0 auto;
  backdrop-filter: blur(25px);
  position: relative;
  overflow: hidden;
}

.cta-box::before {
  content: '';
  position: absolute;
  top: -2px;
  left: -2px;
  right: -2px;
  bottom: -2px;
  background: linear-gradient(45deg, var(--primary-color), var(--accent-color), var(--secondary-color));
  border-radius: 25px;
  z-index: -1;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.cta-box:hover::before {
  opacity: 0.3;
}

.cta-box h2 {
  font-size: clamp(1.8rem, 3vw, 2.5rem);
  font-weight: 700;
  margin-bottom: 20px;
  color: white;
}

.cta-box p {
  font-size: clamp(1rem, 2vw, 1.2rem);
  margin-bottom: 35px;
  opacity: 0.9;
  font-weight: 300;
  line-height: 1.6;
}

.cta-btn {
  background: linear-gradient(135deg, var(--accent-color), #0891b2);
  color: white;
  font-weight: 700;
  font-size: 1.1rem;
  padding: 18px 40px;
  border: none;
  border-radius: 30px;
  text-decoration: none;
  transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  box-shadow: 0 8px 30px rgba(6, 182, 212, 0.4);
  display: inline-block;
  position: relative;
  overflow: hidden;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.cta-btn::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  background: radial-gradient(circle, rgba(255,255,255,0.3) 0%, transparent 70%);
  transition: all 0.4s ease;
  transform: translate(-50%, -50%);
}

.cta-btn:hover {
  transform: translateY(-5px) scale(1.05);
  box-shadow: 0 15px 40px rgba(6, 182, 212, 0.6);
  color: white;
}

.cta-btn:hover::before {
  width: 300px;
  height: 300px;
}

/* Animations */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes fadeInUp {
  from { 
    opacity: 0; 
    transform: translateY(50px); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0); 
  }
}

@keyframes slideInUp {
  from { 
    opacity: 0; 
    transform: translateY(30px) scale(0.95); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0) scale(1); 
  }
}

/* Responsive design */
@media (max-width: 1200px) {
  .feature-section {
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  }
}

@media (max-width: 768px) {
  .login-btns {
    top: 20px;
    right: 20px;
  }
  
  .main-content {
    padding: 60px 15px 30px;
  }
  
  .feature-section {
    grid-template-columns: 1fr;
    gap: 20px;
    padding: 0 10px;
  }
  
  .feature-card {
    padding: 25px;
  }
  
  .cta-box {
    padding: 35px 25px;
    margin: 0 15px;
  }
  
  .cta-btn {
    padding: 15px 30px;
    font-size: 1rem;
  }
}

@media (max-width: 480px) {
  .login-btn {
    padding: 10px 18px;
    font-size: 0.9rem;
  }
  
  .hero-section {
    margin-bottom: 60px;
  }
  
  .feature-card {
    padding: 20px;
  }
  
  .feature-card h3 {
    font-size: 1.2rem;
  }
  
  .cta-box {
    padding: 25px 20px;
  }
}

/* Scrollbar styling */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: var(--darker-color);
}

::-webkit-scrollbar-thumb {
  background: linear-gradient(var(--primary-color), var(--secondary-color));
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: linear-gradient(var(--secondary-color), var(--primary-color));
}
  </style>
</head>
<body>
  <div class="container-wrapper">
    <!-- Animated background elements -->
    <div class="bg-circle circle-1"></div>
    <div class="bg-circle circle-2"></div>
    
    <!-- Login button -->
    <div class="login-btns">
      <a href="/login" class="login-btn">Login</a>
    </div>

    <!-- Main content -->
    <div class="main-content">
      <section class="hero-section">
        <h1>Revolutionize Your Machinery Management</h1>
        <p>
          The ultimate solution for tracking, maintaining, and optimizing your equipment portfolio. 
          Streamline operations, reduce downtime, and make data-driven decisions with our intuitive platform.
        </p>
      </section>

      <section class="feature-section">
        <div class="feature-card">
          <h3><i class="fas fa-clipboard-list"></i> Inventory Tracking</h3>
          <p>Real-time visibility of all your machinery assets with detailed specifications and location tracking.</p>
        </div>
        
        <div class="feature-card">
          <h3><i class="fas fa-tools"></i> Maintenance Logging</h3>
          <p>Schedule, track, and analyze maintenance activities to maximize equipment lifespan and performance.</p>
        </div>
        
        <div class="feature-card">
          <h3><i class="fas fa-chart-line"></i> Performance Monitoring</h3>
          <p>Comprehensive analytics and reporting to identify trends and optimize equipment utilization.</p>
        </div>
        
        <div class="feature-card">
          <h3><i class="fas fa-user-hard-hat"></i> Operator Management</h3>
          <p>Track operator assignments, certifications, and equipment-specific training requirements.</p>
        </div>
        
        <div class="feature-card">
          <h3><i class="fas fa-file-contract"></i> Document Management</h3>
          <p>Centralized storage for manuals, warranties, service records, and compliance documentation.</p>
        </div>
        
        <div class="feature-card">
          <h3><i class="fas fa-bell"></i> Smart Alerts</h3>
          <p>Automated notifications for maintenance schedules, inspections, and critical issues.</p>
        </div>
      </section>

      <section class="cta-section">
        <div class="cta-box">
          <h2>Ready to Transform Your Operations?</h2>
          <p>
            Join industry leaders who trust our platform to manage their valuable equipment assets. 
            Get started today and experience the difference of intelligent machinery management.
          </p>
          <a href="/login" class="cta-btn">Access Your Dashboard Now</a>
        </div>
      </section>
    </div>
  </div>
</body>
</html>