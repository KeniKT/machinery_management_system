<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Welcome | Machinery Management</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
  />

  <style>
    html,
    body {
      height: 100%;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #1b3c6b;
    }

    .split-container {
      display: flex;
      height: 100vh;
      overflow: hidden;
    }

    .left-side {
      flex: 1;
      background: url('/images/bannner.png') center center no-repeat;
      background-size: contain;
      background-repeat: no-repeat;
      background-position: center;
      background-color: #1b3c6b;
    }

    .right-side {
      flex: 1;
      background: linear-gradient(135deg, #1b3c6b, #305c9d);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 40px;
      color: white;
      position: relative;
    }

    .right-box {
      background: transparent;
      padding: 0;
      border-radius: 0;
      width: 100%;
      max-width: 100%;
      animation: fadeIn 1s ease-out;
    }

    .right-box h1 {
      font-size: 2.8rem;
      font-weight: 700;
      margin-bottom: 20px;
    }

    .right-box p {
      font-size: 1.15rem;
      line-height: 1.7;
      margin-bottom: 25px;
    }

    .feature-list {
      list-style: none;
      padding: 0;
      margin-bottom: 30px;
    }

    .feature-list li {
      margin-bottom: 12px;
      font-size: 1.05rem;
    }

    .feature-list li::before {
      content: '✔';
      margin-right: 10px;
      color: #00d1b2;
    }

    .ready-msg {
      margin-top: 40px;
      font-size: 1.3rem;
      font-weight: 500;
      color: #f0f3f7;
      background: rgba(255, 255, 255, 0.08);
      padding: 18px 24px;
      border-left: 6px solid #00d1b2;
      border-radius: 8px;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.25);
      max-width: 100%;
      width: 100%;
    }

    .ready-msg strong {
      display: block;
      font-size: 1.4rem;
      color: #ffffff;
      margin-bottom: 5px;
    }

    .login-btn {
      position: absolute;
      top: 20px;
      right: 30px;
      background: #0d6efd;
      color: white;
      font-weight: 600;
      padding: 10px 24px;
      border: none;
      border-radius: 8px;
      text-decoration: none;
      z-index: 10;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(13, 110, 253, 0.4);
    }

    .login-btn:hover {
      background-color: #0b5ed7;
      box-shadow: 0 4px 12px rgba(13, 110, 253, 0.5);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @media (max-width: 768px) {
      .split-container {
        flex-direction: column;
      }

      .left-side,
      .right-side {
        flex: none;
        height: auto;
        padding: 30px 20px;
      }

      .right-side {
        align-items: center;
        text-align: center;
      }

      .right-box {
        margin: 0 auto;
        padding: 0;
      }

      .ready-msg {
        max-width: 100%;
      }

      .login-btn {
        top: 15px;
        right: 15px;
        padding: 8px 16px;
      }
    }
  </style>
</head>
<body>
  <!-- 🔐 Login Button -->
  <a href="/login" class="login-btn">Login</a>

  <div class="split-container">
    <!-- 📸 Left Side: Image -->
    <div class="left-side"></div>

    <!-- 📄 Right Side: Content -->
    <div class="right-side">
      <div class="right-box">
        <h1>Welcome to the Machinery Management System</h1>
        <p>
          Effortless tracking. Seamless maintenance. Smarter decisions.<br />
          This system is designed to help you manage and monitor your machinery like never
          before — intuitive, efficient, and centralized.
        </p>
        <ul class="feature-list">
          <li>Inventory Tracking</li>
          <li>Maintenance Logging</li>
          <li>Performance Monitoring</li>
          <li>Operator Logs</li>
          <li>Document Management</li>
          <li>Smart Alerts & Notifications</li>
        </ul>
      </div>

      <div class="ready-msg">
        <strong>Ready to Get Started?</strong>
        Click "Login" to access your dashboard and take control of your equipment workflow.
      </div>
    </div>
  </div>
</body>
</html>
